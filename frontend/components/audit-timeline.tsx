import type { Invoice } from "@/types/ap";
export function AuditTimeline({ invoice }: { invoice: Invoice }) { const entries = [...(invoice.reviews ?? [])].reverse(); return <div><h3 className="text-xs font-bold uppercase tracking-[.16em] text-stone-500">Review history</h3><div className="mt-3 border-l border-stone-200 pl-4">{entries.length ? entries.map((r, n) => <div className="relative mb-4" key={`${r.timestamp}-${n}`}><span className="absolute -left-[21px] top-1 size-2 rounded-full bg-[#397357]"/><p className="text-sm font-semibold capitalize">{r.verdict} by {r.reviewer}</p><p className="text-xs text-stone-500">{new Date(r.timestamp).toLocaleString()} {r.reason && `· ${r.reason}`}</p></div>) : <p className="text-sm text-stone-500">No review activity yet.</p>}</div></div> }
