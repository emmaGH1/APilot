"use client";
import type { Invoice } from "@/types/ap";
import { EvidencePanel } from "./evidence-panel";
import { AuditTimeline } from "./audit-timeline";
import { ReviewActions } from "./review-actions";
export function InvoiceDetail({ invoice, onReviewed }: { invoice?: Invoice; onReviewed: () => void }) { if (!invoice) return <section className="grid min-h-96 place-items-center border border-dashed border-stone-300 p-8 text-center text-sm text-stone-500">Select an invoice to inspect its evidence and findings.</section>; return <section className="min-w-0 border border-stone-200 bg-white p-6 md:p-8"><div className="flex flex-wrap items-start justify-between gap-4 border-b border-stone-200 pb-6"><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-[#a66b18]">Human review</p><h2 className="mt-2 font-serif text-3xl">{invoice.vendor}</h2><p className="mt-1 text-sm text-stone-500">{invoice.id} · {invoice.invoice_number} · PO {invoice.po_number ?? "—"}</p></div><p className="font-serif text-2xl">{invoice.currency} {invoice.total.toLocaleString(undefined,{minimumFractionDigits:2})}</p></div><div className="my-6 border-l-2 border-[#d4a04a] bg-[#fffaf1] p-4"><p className="text-xs font-bold uppercase tracking-[.14em] text-[#8b5a16]">Exact findings</p>{invoice.audit?.findings?.length ? invoice.audit.findings.map((f,n)=><p className="mt-2 text-sm" key={n}>{f.message ?? `${f.field ?? "Value"}: ${String(f.actual)} (expected ${String(f.expected)})`}</p>) : <p className="mt-2 text-sm text-stone-600">No recorded findings.</p>}</div><div className="grid gap-8 xl:grid-cols-2"><EvidencePanel invoice={invoice}/><AuditTimeline invoice={invoice}/></div><div className="mt-8"><ReviewActions id={invoice.id} onSuccess={onReviewed}/></div></section> }
