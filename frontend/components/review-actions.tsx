"use client";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Textarea } from "@/components/ui/textarea";
import { reviewInvoice } from "@/lib/api";
export function ReviewActions({ id, onSuccess }: { id: string; onSuccess: () => void }) { const [reason,setReason] = useState(""); const [busy,setBusy] = useState(false); const act = async (verdict: "approve"|"hold"|"escalate") => { setBusy(true); try { await reviewInvoice(id, verdict, reason); setReason(""); onSuccess(); } finally { setBusy(false); } }; return <div className="border-t border-stone-200 pt-5"><label className="text-xs font-bold uppercase tracking-[.16em] text-stone-500" htmlFor="reason">Reason <span className="font-normal normal-case tracking-normal">(optional)</span></label><Textarea id="reason" value={reason} onChange={e=>setReason(e.target.value)} className="mt-2 min-h-20 border-stone-200" placeholder="Add context for the audit trail" disabled={busy}/><div className="mt-3 flex flex-wrap gap-2"><Button disabled={busy} onClick={()=>act("approve")} className="bg-[#397357] hover:bg-[#2c5b43]">Approve</Button><Button disabled={busy} onClick={()=>act("hold")} variant="outline" className="border-[#d4a04a] text-[#8b5a16]">Hold</Button><Button disabled={busy} onClick={()=>act("escalate")} variant="outline" className="border-[#bd6d5d] text-[#a34636]">Escalate</Button></div></div> }
